# Google Cloud 設定
# ===================

# Google Cloud 專案 ID
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Google Cloud Service Account 金鑰檔案路徑
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Google Cloud Storage Bucket 名稱（用於暫存音檔）
GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name

# Gemini API 金鑰（用於 AI 分析功能）
GEMINI_API_KEY=your-gemini-api-key

# 資料庫設定
# ===================

# PostgreSQL 連線字串
DATABASE_URL=postgresql://username:password@localhost:5432/voicetextpro

# Gmail SMTP 設定（用於發送驗證郵件和密碼重設郵件）
# ===================
# 注意：需要使用 Gmail 應用程式密碼，不是登入密碼
# 設定步驟：
# 1. 前往 Google 帳號設定 > 安全性 > 兩步驟驗證（需先開啟）
# 2. 在兩步驟驗證頁面底部，點擊「應用程式密碼」
# 3. 選擇「郵件」和「其他」，輸入名稱如「VoiceTextPro」
# 4. 複製產生的 16 位密碼
GMAIL_USER=your-gmail@gmail.com
GMAIL_PASSWORD=your-16-digit-app-password

# 應用程式網址（用於產生驗證連結和密碼重設連結）
APP_URL=http://localhost:3000

# 推送通知 VAPID 金鑰
# ===================
# 可使用 npx web-push generate-vapid-keys 產生
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=mailto:admin@example.com

# Session 設定
# ===================

# Session 加密金鑰（請使用隨機生成的字串）
SESSION_SECRET=your-random-session-secret

# Google Calendar OAuth 設定
# ===================
# 設定步驟：
# 1. 前往 Google Cloud Console > APIs & Services > Credentials
# 2. 建立 OAuth 2.0 Client ID（應用程式類型：Web application）
# 3. 設定授權重新導向 URI（如：http://localhost:5000/api/google/auth/callback）
# 4. 複製 Client ID 和 Client Secret

GOOGLE_CLIENT_ID=your-oauth-client-id
GOOGLE_CLIENT_SECRET=your-oauth-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5000/api/google/auth/callback

# Token 加密金鑰（用於加密儲存 OAuth tokens）
# 必須是 64 個 hex 字元（32 bytes）
# 可使用以下命令生成：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GOOGLE_TOKEN_ENCRYPTION_KEY=your-64-hex-character-encryption-key

# 其他設定
# ===================

# 應用程式埠號
PORT=5000

# 環境模式 (development / production)
NODE_ENV=development
