# Google Cloud 設定
# ===================

# Google Cloud 專案 ID
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Google Cloud Service Account 金鑰檔案路徑
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Google Cloud Storage Bucket 名稱（用於暫存音檔）
GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name

# Gemini API 金鑰（用於 AI 分析功能）
GEMINI_API_KEY=your-gemini-api-key

# 資料庫設定
# ===================

# Docker Compose 用的獨立變數
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=voicetextpro

# PostgreSQL 連線字串（本地開發用，Docker 會自動組合）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/voicetextpro

# 郵件服務設定 (Gmail SMTP)
# ===================

# Gmail SMTP 設定（用於發送驗證郵件和密碼重設郵件）
# 注意：需要使用 Gmail 應用程式密碼，不是登入密碼
# 設定步驟：
# 1. 前往 Google 帳號設定 > 安全性 > 兩步驟驗證（需先開啟）
# 2. 在兩步驟驗證頁面底部，點擊「應用程式密碼」
# 3. 選擇「郵件」和「其他」，輸入名稱如「VoiceTextPro」
# 4. 複製產生的 16 位密碼
GMAIL_USER=your-gmail@gmail.com
GMAIL_PASSWORD=your-16-digit-app-password

# 應用程式網址（用於產生驗證連結和密碼重設連結）
# Docker 模式請使用 http://localhost:5000
APP_URL=http://localhost:5000

# Session 設定
# ===================

# Session 加密金鑰（請使用隨機生成的字串）
SESSION_SECRET=your-random-session-secret

# 管理員設定
# ===================

# 預設管理員 Email（首次啟動時自動創建）
ADMIN_EMAIL=admin@example.com

# 預設管理員密碼（首次啟動時使用，建議登入後立即修改）
ADMIN_PASSWORD=admin123

# 其他設定
# ===================

# 應用程式埠號
PORT=5000

# 環境模式 (development / production)
NODE_ENV=development
